#include <stdio.h>

/**
 * main - prints alpha in lower case and upper case
 * Return: Always 0
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; c++)
		putchar(ch);

	for (ch = 'A'; ch <= 'Z'; c++)
		putchar(ch);

	putchar('\n');

	return (0);
}
